<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="hr_loan_comp_rule" model="ir.rule">
            <field name="name">Loan multi-company</field>
            <field name="model_id" ref="model_hr_loan"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'parent_of', company_ids)]</field>
        </record>

        <!-- Rule granting access to HR users to modify loan forms -->
        <record id="hr_loan_rule_officer" model="ir.rule">
            <field name="name">Loan Officer (Clear User Own Only)</field>
            <field name="model_id" ref="model_hr_loan"/>
            <field name="groups" eval="[(4, ref('ohrms_loan.group_loan_officer'))]"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="1"/>
        </record>
        <!-- Rule allowing employees to modify their own loan records -->
        <record id="hr_loan_rule_employee" model="ir.rule">
            <field name="name">User: Modify own loan only</field>
            <field name="model_id" ref="model_hr_loan"/>
            <field name="groups" eval="[(4,ref('base.group_user'))]"/>
            <field name="domain_force">[('employee_id.user_id','=',user.id)]</field>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="1"/>
        </record>
    </data>
</odoo>
